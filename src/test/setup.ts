import * as activies from "../activities/priceActionPosition";
import * as dotenv from "dotenv";
dotenv.config();

// beforeAll(async () => {
//   const clientId = process.env.TD_CLIENT_ID;
//   const credentials = await activies.getLoginCredentials(clientId);
//   expect(typeof credentials).toBe("string");
// });